int countWords(string str){

        int i,count=1;;
        for(i=0;i<=str.length();i++)
        {
           if(str[i]==' ')
           {
               
               count++;
           }
        }
        return count;
    }
