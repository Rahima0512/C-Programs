int gcd(int a, int b){

        
    int ans=1;
    int end=max(a,b);
    for(int i=1;i<=end;i++)
    {
        if(a%i==0&&b%i==0)
        {
            ans=i;
        }
    }

    return ans; 

        
}
