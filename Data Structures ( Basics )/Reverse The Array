void reverseArray(int arr[], int n) {
    
    int low=0,high=n-1;
    while(low<high)
    {
        int temp=0;
        temp=arr[low];
        arr[low]=arr[high];
        arr[high]=temp;
        high--;
        low++;
        
    }
}
